# 3. Design

Helper

<img width="390" height="390" alt="image" src="https://github.com/user-attachments/assets/89feb52e-bf70-4e62-b08a-430b5d2f819f" />


22212032 강민교

Email : wells7845@naver.com

## Revision history

| Revision date | Version # | Description | Author |
| --- | --- | --- | --- |
| 03/25/2026 | 1.01 | Use Case 및 Concept of Operation | mingyo |
| 05/08/2026 | 1.02 | Use case description, Domain analysis, User Interface prototype | mingyo |
| 05/23/2026 | 1.03 | class diagram, state machine diagram, sequence diagram 수정 | mingyo |

## Contents

- Introduction
- 2. Class diagram
- 3. Sequence diagram
- 4. State machine diagram
- 5. Implementation requirements
- 6. Glossary
- 7. References

## Introduction

본 앱은 주방 근무자를 위한 Kitchen Helper 앱으로, 직원 로그인, 업무 체크리스트, 조리 타이머, 관리자 설정, 사용자 프로필 기능을 제공한다. 사용자는 사원번호를 입력하여 로그인할 수 있으며, 일반 직원은 업무 확인과 조리 보조 기능을 사용할 수 있고, 매니저는 관리자 화면을 통해 공지사항, 업무 템플릿, 스태프 계정 관리 기능에 접근할 수 있다.

이번 디자인 설계에서는 Android Jetpack Compose를 기반으로 화면을 구성하였으며, 각 기능을 역할별로 분리하여 클래스 다이어그램을 작성하였다. LoginUser, TaskItem, CookingTimerState와 같은 데이터 클래스는 Model 역할을 하고, LoginScreen, HomeScreen, TaskScreen, CookingScreen, ManageScreen, ProfileScreen은 사용자에게 화면을 보여주는 View 역할을 한다. 또한 MainScreen은 로그인 상태, 화면 전환, 사용자 권한 확인을 담당하므로 Controller 역할로 표현하였다.

이와 같이 Compose의 화면 단위 구조를 활용하여 UI, 상태 데이터, 화면 이동 제어를 분리함으로써 앱의 전체 구조를 명확하게 표현하고, 각 클래스가 어떤 책임을 가지는지 쉽게 파악할 수 있도록 설계하였다.

## 2. Class diagram

<img width="1878" height="921" alt="image" src="https://github.com/user-attachments/assets/f893d832-6150-4555-a4a4-2bbfde451527" />



<img width="563" height="126" alt="image" src="https://github.com/user-attachments/assets/e177c028-2af3-4bbd-a802-512b3168df9b" />


### 1) MainActivity

| 구분 | 내용 |
| --- | --- |
| Attributes | 없음 |
| Methods | - onCreate(savedInstanceState: Bundle?): void: 앱이 실행될 때 호출되며 Compose 화면을 초기화하고 MainScreen을 실행한다. |
| Description | MainActivity 클래스는 앱의 시작점 역할을 한다. Android Activity로서 앱이 실행되면 HelperTheme을 적용하고 전체 화면 흐름을 담당하는 MainScreen을 호출한다. |

<img width="266" height="176" alt="image" src="https://github.com/user-attachments/assets/944a50a7-2636-41ae-9f3f-4c3356ba479a" />


### 2) MainScreen

| 구분 | 내용 |
| --- | --- |
| Attributes | - navController: NavController : 화면 이동을 관리하는 네비게이션 컨트롤러<br>- snackbarHostState: SnackbarHostState : 사용자에게 안내 메시지를 표시하기 위한 상태 객체<br>- user: LoginUser? : 현재 로그인한 사용자 정보<br>- selectedTab: HelperTab? : 현재 선택된 하단 탭 정보 |
| Methods | - MainScreen(): void : 앱의 전체 화면 구조, 로그인 상태, 네비게이션, 하단 메뉴를 관리한다.<br>- LoginRoute(): void : 로그인 화면의 상태를 관리하고 사원번호 검증 및 로그인 처리를 수행한다.<br>- TaskRoute(): void : 업무 목록 상태를 관리하고 체크 여부 변경을 처리한다.<br>- CookingRoute(): void : 조리 타이머 상태를 관리하고 시작, 정지, 초기화 기능을 처리한다. |
| Description | MainScreen은 앱 전체의 Controller 역할을 한다. 로그인 여부에 따라 화면을 전환하고, 사용자의 권한에 따라 관리자 화면 접근을 제어하며, 각 화면으로 이동하는 네비게이션 흐름을 관리한다. |

<img width="342" height="155" alt="image" src="https://github.com/user-attachments/assets/03a99300-ef77-4ad5-881b-d96a48040588" />


### 3) LoginScreen

| 구분 | 내용 |
| --- | --- |
| Attributes | - formState: LoginFormState : 로그인 입력 상태<br>- branchMenuOpen: Boolean : 지점 선택 메뉴가 열려 있는지 나타내는 상태 |
| Methods | - LoginScreen(formState: LoginFormState): void : 로그인 화면을 구성한다.<br>- onBranchChange(branch: String): void : 사용자가 선택한 지점 정보를 변경한다.<br>- onAccessCodeChange(accessCode: String): void : 사용자가 입력한 사원번호를 변경한다.<br>- onSubmit(): void : 로그인 버튼 클릭 시 로그인 요청을 전달한다. |
| Description | LoginScreen 클래스는 사용자가 지점과 사원번호를 입력하여 로그인할 수 있도록 하는 Boundary/View 역할을 한다. 실제 로그인 검증은 직접 수행하지 않고, 입력값과 사용자 이벤트를 상위 Controller인 MainScreen으로 전달한다. |

<img width="307" height="165" alt="image" src="https://github.com/user-attachments/assets/a9ce66ef-b389-4410-9b0f-afbac4503973" />


### 4) LoginFormState

| 구분 | 내용 |
| --- | --- |
| Attributes | - branch: String : 사용자가 선택한 지점 이름<br>- accessCode: String : 사용자가 입력한 사원번호 |
| Methods | 없음 |
| Description | LoginFormState 클래스는 로그인 화면에서 입력되는 지점과 사원번호 상태를 저장하는 데이터 클래스이다. 로그인 입력 폼의 현재 상태를 표현한다. |

<img width="285" height="191" alt="image" src="https://github.com/user-attachments/assets/0ec118d9-2b8a-4fb2-bfc6-5d15c64d9d59" />


### 5) LoginUser

| 구분 | 내용 |
| --- | --- |
| Attributes | - name: String : 사용자 이름<br>- role: String : 사용자 역할<br>- isManager: Boolean : 사용자가 매니저인지 여부 |
| Methods | 없음 |
| Description | LoginUser 클래스는 로그인한 사용자의 정보를 저장하는 데이터 클래스이다. 사용자 이름과 역할을 가지고 있으며, 역할이 Manager인지 확인하여 관리자 기능 접근 여부를 판단하는 데 사용된다. |

<img width="465" height="186" alt="image" src="https://github.com/user-attachments/assets/291f724d-2224-4521-a81c-da617c282d39" />


### 6) HomeScreen

| 구분 | 내용 |
| --- | --- |
| Attributes | - user: LoginUser : 현재 로그인한 사용자 정보 |
| Methods | - HomeScreen(user: LoginUser): void : 홈 화면을 표시한다.<br>- NoticeCard(): void : 오늘의 특이사항 안내 카드를 표시한다. |
| Description | HomeScreen 클래스는 로그인한 사용자에게 인사말, 오늘의 업무 달성률, 특이사항을 보여주는 메인 화면 역할을 한다. 사용자 정보를 기반으로 개인화된 홈 화면을 구성한다. |

<img width="552" height="179" alt="image" src="https://github.com/user-attachments/assets/e7222ebc-2b04-4508-ba4d-9e986d8a3fb2" />


### 7) TaskScreen

| 구분 | 내용 |
| --- | --- |
| Attributes | - tasks: List<TaskItem> : 화면에 표시할 업무 목록<br>- doneCount: Int : 완료된 업무 개수 |
| Methods | - TaskScreen(tasks: List<TaskItem>): void : 업무 체크리스트 화면을 구성한다.<br>- onTaskCheckedChange(taskId: Int, checked: Boolean): void : 특정 업무의 완료 상태 변경을 요청한다.<br>- TaskRow(item: TaskItem): void : 개별 업무 항목을 한 줄로 표시한다. |
| Description | TaskScreen 클래스는 주방 업무 체크리스트를 표시하는 Boundary/View 역할을 한다. 업무 목록을 보여주고 사용자가 체크박스를 변경하면 해당 변경 이벤트를 상위 Controller로 전달한다. |

<img width="302" height="218" alt="image" src="https://github.com/user-attachments/assets/4b3eab20-a395-4e23-a0fe-2c6e02b23ae9" />


### 8) TaskItem

| 구분 | 내용 |
| --- | --- |
| Attributes | - id: Int : 업무 항목의 고유 번호<br>- title: String : 업무 내용<br>- done: Boolean : 업무 완료 여부 |
| Methods | 없음 |
| Description | TaskItem 클래스는 하나의 업무 항목을 표현하는 데이터 클래스이다. 각 업무의 번호, 제목, 완료 여부를 저장하며 TaskScreen에서 체크리스트 항목으로 사용된다. |

<img width="547" height="201" alt="image" src="https://github.com/user-attachments/assets/ec91de58-004e-48f9-8551-0e8c2157d85d" />


### 9) CookingScreen

| 구분 | 내용 |
| --- | --- |
| Attributes | - timerState: CookingTimerState : 조리 타이머의 현재 상태 |
| Methods | - CookingScreen(timerState: CookingTimerState): void : 조리 가이드와 타이머 화면을 표시한다.<br>- onResetTimer(): void : 타이머 초기화를 요청한다.<br>- onToggleTimer(): void : 타이머 시작 또는 일시정지를 요청한다.<br>- formatTime(seconds: Int): String : 초 단위 시간을 분:초 형식의 문자열로 변환한다. |
| Description | CookingScreen 클래스는 조리 가이드와 타이머를 표시하는 Boundary/View 역할을 한다. 현재 조리 단계와 남은 시간을 보여주며, 사용자가 타이머를 시작, 일시정지, 초기화할 수 있도록 한다. |

<img width="309" height="190" alt="image" src="https://github.com/user-attachments/assets/2db27a95-4c8c-4331-9dfd-5d69a25e8551" />


### 10) CookingTimerState

| 구분 | 내용 |
| --- | --- |
| Attributes | - remainingSeconds: Int : 타이머에 남은 시간<br>- running: Boolean : 타이머가 실행 중인지 여부 |
| Methods | 없음 |
| Description | CookingTimerState 클래스는 조리 타이머의 상태를 저장하는 데이터 클래스이다. 남은 시간과 실행 여부를 관리하며 CookingScreen과 CookingRoute에서 사용된다. |

<img width="642" height="127" alt="image" src="https://github.com/user-attachments/assets/f655865d-6eaa-42e8-9d35-0cf7b12541d6" />


### 11) ManageScreen

| 구분 | 내용 |
| --- | --- |
| Attributes | 없음 |
| Methods | - ManageScreen(): void : 관리자 설정 화면을 표시한다.<br>- ManageItem(title: String, description: String, action: String): void : 관리자 기능 항목을 카드 형태로 표시한다. |
| Description | ManageScreen 클래스는 매니저 전용 관리 화면을 담당한다. 공지사항 관리, 업무 템플릿 수정, 스태프 계정 관리와 같은 관리자 기능을 사용자에게 보여주는 Boundary/View 역할을 한다. |

<img width="468" height="209" alt="image" src="https://github.com/user-attachments/assets/337f547a-a248-4ace-9e42-0fe20b27101f" />


### 12) ProfileScreen

| 구분 | 내용 |
| --- | --- |
| Attributes | - user: LoginUser : 현재 로그인한 사용자 정보 |
| Methods | - ProfileScreen(user: LoginUser): void : 프로필 화면을 표시한다.<br>- onLogout(): void : 로그아웃 요청을 처리한다.<br>- ProfileAction(title: String): void : 프로필 관련 기능 항목을 표시한다. |
| Description | ProfileScreen 클래스는 현재 로그인한 사용자의 이름과 직급 정보를 보여주는 프로필 화면이다. 비밀번호 변경, 알림 설정, 로그아웃 기능을 제공하며 로그아웃 이벤트를 상위 Controller로 전달한다. |

<img width="398" height="165" alt="image" src="https://github.com/user-attachments/assets/18eab2fb-1f65-443d-abb8-eaa5f70bd37d" />


### 13) HelperDefaults

| 구분 | 내용 |
| --- | --- |
| Attributes | - users: Map<String, LoginUser> : 사원번호와 사용자 정보를 저장한 기본 사용자 목록 |
| Methods | - tasks(): List<TaskItem> : 기본 업무 목록을 생성하여 반환한다. |
| Description | HelperDefaults 클래스는 앱에서 사용하는 기본 데이터를 제공하는 객체이다. 로그인 가능한 사용자 목록과 초기 업무 체크리스트 데이터를 관리하며, 실제 데이터베이스 대신 샘플 데이터 저장소 역할을 한다. |

<img width="313" height="217" alt="image" src="https://github.com/user-attachments/assets/afe7ec15-2d92-44e9-92e9-4356be18ff49" />


### 14) HelperTab

| 구분 | 내용 |
| --- | --- |
| Attributes | - label: String : 하단 탭에 표시될 이름<br>- icon: ImageVector : 하단 탭에 표시될 아이콘<br>- route: Any : 화면 이동에 사용되는 경로 객체<br>- routeClass: KClass<*> : 현재 화면과 탭을 비교하기 위한 라우트 클래스 정보 |
| Methods | 없음 |
| Description | HelperTab 클래스는 앱 하단 네비게이션 탭 정보를 정의하는 enum 클래스이다. 홈, 업무, 조리, 관리, 프로필 탭의 이름, 아이콘, 이동 경로를 관리한다. |

## 3. Sequence diagram

### 1. App Launch Description

<img width="434" height="391" alt="image" src="https://github.com/user-attachments/assets/74e78b79-5753-48bb-9950-b7d414dfc1bf" />


이 시퀀스 다이어그램은 사용자가 앱을 실행한 뒤 초기 로그인 화면이 표시되기까지의 흐름을 나타낸다. MainActivity는 앱 실행 시 onCreate()를 호출하고, setContent를 통해 Compose UI를 설정한다. 이후 HelperTheme을 적용한 상태에서 MainScreen을 실행하며, MainScreen은 NavController와 NavHost를 생성한다. NavHost의 시작 화면이 Login으로 설정되어 있기 때문에 최종적으로 LoginScreen이 사용자에게 표시된다.

### 2. Login Input Description

<img width="420" height="433" alt="image" src="https://github.com/user-attachments/assets/55a24e76-caa6-4ae4-9768-a9e691603b6d" />



이 시퀀스 다이어그램은 사용자가 로그인 화면에서 지점을 선택하고 사원번호를 입력할 때 로그인 입력 상태가 갱신되는 과정을 나타낸다. 사용자가 지점 버튼을 클릭하면 LoginScreen은 지점 선택 메뉴를 열고, 선택된 지점 정보를 LoginRoute로 전달한다. 또한 사용자가 사원번호를 입력하면 LoginScreen은 숫자만 필터링하고 4자리까지만 입력되도록 처리한 뒤, 변경된 값을 LoginRoute로 전달한다. LoginRoute는 전달받은 branch와 accessCode 값을 이용해 LoginFormState를 갱신하고, 변경된 입력 상태가 다시 로그인 화면에 반영된다.

### 3. Login Success

<img width="642" height="463" alt="image" src="https://github.com/user-attachments/assets/5d9fa856-27a6-4def-95e8-4145c09a9185" />


이 시퀀스 다이어그램은 사용자가 로그인 버튼을 누른 뒤 정상적으로 로그인에 성공하여 홈 화면으로 이동하는 과정을 나타낸다. LoginScreen은 사용자의 로그인 요청을 LoginRoute로 전달하고, LoginRoute는 LoginFormState에 저장된 지점과 사원번호를 확인한다. 이후 HelperDefaults의 사용자 목록에서 입력된 사원번호에 해당하는 LoginUser를 찾고, 사용자가 존재하면 MainScreen의 onLogin을 호출한다. MainScreen은 로그인한 사용자 정보를 저장한 뒤 NavController를 통해 HomeScreen으로 화면을 전환한다.

### 4. Login Failure

<img width="642" height="533" alt="image" src="https://github.com/user-attachments/assets/7f62bf5a-1767-42ed-82b4-f3ecf6783fb2" />


이 시퀀스 다이어그램은 사용자가 로그인 버튼을 눌렀지만 로그인에 실패하는 과정을 나타낸다. LoginScreen은 로그인 요청을 LoginRoute로 전달하고, LoginRoute는 LoginFormState에 저장된 지점과 사원번호를 확인한다. 선택한 지점이 하양점이거나, 사원번호가 4자리가 아니거나, 입력한 사원번호에 해당하는 사용자가 HelperDefaults에 존재하지 않는 경우 로그인은 진행되지 않는다. 이때 SnackbarHostState를 통해 오류 메시지가 표시되고, 사용자는 다시 로그인 화면에 머무르게 된다.

### 5. Bottom Tab Navigation

<img width="642" height="401" alt="image" src="https://github.com/user-attachments/assets/62210c3a-d582-4d0b-a8e4-c81a8adb3361" />


이 시퀀스 다이어그램은 로그인 후 사용자가 하단 탭을 선택하여 다른 화면으로 이동하는 과정을 나타낸다. HelperBottomBar는 사용자가 선택한 탭 정보를 HelperTab을 통해 확인하고, 선택 이벤트를 MainScreen으로 전달한다. MainScreen은 전달받은 탭의 route 정보를 이용하여 NavController.navigate를 실행하고, 그 결과 선택한 화면인 TaskScreen이 사용자에게 표시된다.

### 6. Task List Load

<img width="642" height="498" alt="image" src="https://github.com/user-attachments/assets/791bfdc9-c617-4c9a-87b1-6c57ed59a9dc" />


이 시퀀스 다이어그램은 사용자가 업무 탭을 선택한 뒤 기본 업무 목록이 생성되고 화면에 표시되는 과정을 나타낸다. MainScreen은 NavController를 통해 업무 화면으로 이동하고, TaskRoute를 실행한다. TaskRoute는 HelperDefaults.tasks를 호출하여 기본 업무 목록을 가져오며, 각 업무는 TaskItem 객체로 구성된다. 이후 생성된 업무 목록은 TaskScreen으로 전달되고, TaskScreen은 완료된 업무 개수를 계산한 뒤 각 업무 항목을 TaskRow 형태로 반복 표시한다.

### 7. Task Check Update

<img width="642" height="668" alt="image" src="https://github.com/user-attachments/assets/14b1537f-93df-4991-b904-98bc07b6682e" />


이 시퀀스 다이어그램은 사용자가 업무 체크리스트에서 체크박스를 클릭했을 때 업무 완료 상태가 변경되는 과정을 나타낸다. TaskRow는 사용자의 체크 이벤트를 받고, 이를 TaskScreen의 onTaskCheckedChange로 전달한다. TaskScreen은 변경할 업무의 ID와 체크 상태를 TaskRoute로 전달하며, TaskRoute는 업무 목록을 순회하면서 선택된 TaskItem을 찾아 done 값을 갱신한다. 이후 변경된 업무 목록이 다시 TaskScreen에 반영되어 사용자에게 갱신된 체크리스트가 표시된다.

### 8. Cooking Timer

<img width="642" height="563" alt="image" src="https://github.com/user-attachments/assets/8f53f42a-7e18-4558-9dbb-0f243c86c778" />


이 시퀀스 다이어그램은 사용자가 조리 타이머의 시작 버튼을 눌렀을 때 타이머가 실행되고 남은 시간이 감소하는 과정을 나타낸다. CookingScreen은 사용자의 버튼 클릭 이벤트를 CookingRoute로 전달하고, CookingRoute는 CookingTimerState의 running 값을 변경한다. 이후 LaunchedEffect는 타이머가 실행 중이고 남은 시간이 0보다 클 때 1초마다 remainingSeconds 값을 감소시킨다. 변경된 타이머 상태는 다시 CookingScreen에 반영되어 사용자에게 갱신된 남은 시간이 표시되며, 남은 시간이 0이 되면 running 값이 false로 변경되어 타이머가 정지된다.

### 9. Profile Logout

<img width="642" height="611" alt="image" src="https://github.com/user-attachments/assets/c0a006eb-01d3-4909-8c25-e74f18b33142" />


이 시퀀스 다이어그램은 사용자가 프로필 화면에서 로그아웃을 수행하는 과정을 나타낸다. ProfileScreen은 현재 로그인한 사용자의 정보를 표시하고, 사용자가 로그아웃 버튼을 누르면 onLogout 콜백을 통해 MainScreen에 로그아웃 요청을 전달한다. MainScreen은 현재 사용자 상태를 null로 변경하여 로그인 상태를 해제하고, NavController를 통해 LoginScreen으로 화면을 전환한다. 이때 기존 화면 스택을 정리하여 로그아웃 후 다시 이전 화면으로 돌아가지 않도록 처리한다.

## 4. State machine diagram

### 1) Cooking Timer State Machine Diagram

<img width="642" height="562" alt="image" src="https://github.com/user-attachments/assets/f78c2a27-e2dd-4d7b-b3be-8824e0779566" />


이 State Machine Diagram은 조리 타이머의 상태 변화를 나타낸다. 타이머는 초기 상태에서 Idle 상태로 시작하며, 사용자가 시작 버튼을 누르면 Running 상태로 전환된다. Running 상태에서는 1초마다 remainingSeconds가 감소하고, 사용자가 일시정지 버튼을 누르면 Paused 상태로 이동한다. Paused 상태에서 다시 시작하면 Running 상태로 돌아가며, 남은 시간이 0초가 되면 Finished 상태가 된다. 사용자가 초기화 버튼을 누르면 타이머는 다시 기본 시간으로 설정되어 Idle 상태로 돌아간다.

### 2) Task Item

<img width="642" height="291" alt="image" src="https://github.com/user-attachments/assets/24769c23-f5cf-45a0-bfeb-3bdd69b37ef9" />


이 State Machine Diagram은 업무 체크리스트 항목인 TaskItem의 완료 상태 변화를 나타낸다. 각 업무 항목은 done 값을 통해 완료 여부를 표현하며, 초기에는 미완료 상태인 Not Done으로 시작한다. 사용자가 체크박스를 선택하면 done 값이 true로 변경되어 Done 상태가 되고, 다시 체크를 해제하면 done 값이 false로 변경되어 Not Done 상태로 돌아간다. 이를 통해 하나의 업무 항목이 사용자 입력에 따라 완료 상태와 미완료 상태를 반복적으로 전환하는 과정을 표현한다.

### 3) App Login

<img width="386" height="421" alt="image" src="https://github.com/user-attachments/assets/cc78188a-619d-441c-be9b-bb1dcb4c256c" />


이 State Machine Diagram은 앱의 로그인 상태 변화를 나타낸다. 앱이 처음 실행되면 현재 로그인한 사용자가 없으므로 Logged Out 상태에서 시작하며, 사용자에게 LoginScreen이 표시된다. 사용자가 지점과 사원번호를 입력하고 로그인 버튼을 누르면 Validating Login 상태로 이동하여 입력값을 검사한다. 입력한 사원번호에 해당하는 사용자가 존재하면 Logged In 상태가 되고, MainScreen은 로그인한 사용자 정보를 저장한 뒤 홈 화면을 표시한다. 반대로 지점이 준비되지 않았거나 사원번호 형식이 올바르지 않거나 등록된 사용자가 없으면 Login Failed 상태가 되어 오류 메시지를 표시하고 다시 로그인 입력 상태로 돌아간다. 이후 사용자가 로그아웃하면 사용자 정보가 초기화되어 다시 Logged Out 상태가 된다.

## 5. Implementation requirements

### S/W Requirements

- (1) OS : Windows 10+
- (2) IDE : Android Studio
- (3) Android SDK : API 21+
- (4) Framework : Android Jetpack Compose
- (5) Implement Language : Kotlin
- (6) Build Tool : Gradle

### H/W Requirements

- (1) Android 기반 스마트 기기
- (2) Android Studio 기본 Emulator 실행 가능 PC
- (3) Galaxy S6 또는 동급 이상의 Android 기기

## 6. Glossary

Jetpack Compose : Android 앱의 UI를 선언형 방식으로 구성하는 UI 프레임워크이다.

Composable : Jetpack Compose에서 화면이나 UI 요소를 구성하는 함수이다.

## 7. References

Android Developers, Jetpack Compose Documentation

본 프로젝트의 UI 설계 및 Compose 화면 구성 방식 참고
